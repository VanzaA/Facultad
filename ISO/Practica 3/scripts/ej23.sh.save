#! /bin/bash
num=(10 3 5 7 9 3 5 4)
imp=0
for ((i = 0; i < ${#num[@]}; i++))
do
	n=`expr num[$1] % 2`
	if [ $n -eq 0 ]
	the
		echo num[$i]
	else
		imp=`expr $imp + num[$i]`
	fi
done
echo "la cantidad de impares es" $imp
