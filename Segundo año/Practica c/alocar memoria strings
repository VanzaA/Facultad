char * s= malloc(1);
int i=0;
s='\0';
while((c=getchar())!=EOF){
  s[i]=c;
  s=realloc(s,strlen(s)+1);
}
--------------------------------------------------------------------------------
--------------------------------------------------------------------------------
--------------------------------------------------------------------------------
int j=1000
char * s=malloc(j);
int i=0;
s='\0';
while((c=getchar())!=EOF){
    if(i+2>j){
      j=j*2;
      realloc(s,j);
      }
  s[i]=c
}
s=realloc(s,i+2);
