#include <stdio.h>
#include "matem.h"
int main(){
  int a=1;
  int b=2;
  printf("la suma de %d y %d es igual a: %d \n",a,b,suma(a,b));
  printf("bimbo tostado es una bestia sexual ;););));;);););) \n");
  return 0;
}
