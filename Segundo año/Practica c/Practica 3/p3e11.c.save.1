#include <string.h>

int es_palindromo(const char *word){
  int ok = 1,i=0;
  unsigned p = strlen(word)-1;
  printf("%d \n",p);
  while((ok)&&(p>=i)){
    if (word[i] == word[p])
      ok = 1;
    else
      ok = 0;
    i++;
    p--;
  }
  return ok;
}

